{% extends '::base.html.twig' %}

{% block body -%}
    <h1>SkillPath creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('skillpath') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
